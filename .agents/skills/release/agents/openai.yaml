interface:
  display_name: "Release"
  short_description: "Publish and install a verified tsm release"
  default_prompt: "Use $release to publish and install the next verified tsm release."
policy:
  allow_implicit_invocation: false
